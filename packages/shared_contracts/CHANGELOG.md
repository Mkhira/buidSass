# `shared_contracts` Changelog

Version bumps happen in CI on every merge to `main` (`0.0.${GITHUB_RUN_NUMBER}`). This file records **shape changes** only — added/removed/changed paths, schemas, and breaking-change markers.

Format loosely follows [Keep a Changelog](https://keepachangelog.com). Published surface covers `.NET` (NuGet `BuidSass.SharedContracts`) and `npm` (`@mkhira/shared-contracts`); Dart is deferred to Phase 1.5.

## [Unreleased]

- _No shape changes yet. Spec 004 will land the first real endpoints._

## 0.0.23 — 2026-04-19

- Initial publish from an empty OpenAPI skeleton (spec 003 T077 verification).
- No paths, no schemas. Exists to prove the publish pipeline end-to-end.
